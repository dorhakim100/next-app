'use client'
import React from 'react'

const AddToCart = () => {
  return (
    <button
      className='btn btn-primary'
      onClick={() => alert('clicked')}
    >
      AddToCart
    </button>
  )
}

export default AddToCart
